# Run A/B — dominio DISEÑO (verificación por normas) con Phi-4-mini.
extends: base_phi4.yaml

run_name: nodex-design-phi4

data:
  dataset_dir: ../nodex-code/datasets/design-sft-4000

train:
  output_dir: adapters/design-B
  num_train_epochs: 2        # subset A/B: basta para comparar el "feel"
