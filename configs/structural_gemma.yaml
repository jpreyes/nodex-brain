# Run A/B — dominio estructural con Gemma.
extends: base_gemma.yaml

run_name: nodex-structural-gemma

data:
  dataset_dir: datasets/structural-sft-1000

train:
  output_dir: adapters/structural-gemma
