# Run A/B — dominio estructural con Mistral.
extends: base_mistral.yaml

run_name: nodex-structural-mistral

data:
  dataset_dir: datasets/structural-sft-1000

train:
  output_dir: adapters/structural-mistral
