# Run A/B — dominio DISEÑO (verificación por normas) con el base A.
extends: base_12b_a.yaml

run_name: nodex-design-A

data:
  dataset_dir: ../nodex-code/datasets/design-sft-4000

train:
  output_dir: adapters/design-A
  num_train_epochs: 2        # subset A/B: basta para comparar el "feel"
